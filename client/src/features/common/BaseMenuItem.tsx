//  ======================================== IMPORTS
import MenuItem from '@material-ui/core/MenuItem';
import { withStyles } from '@material-ui/core/styles';

const BaseMenuItem = withStyles(() => ({
  root: {
  },
}))(MenuItem);

export default BaseMenuItem;
